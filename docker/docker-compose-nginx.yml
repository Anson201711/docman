# ============================================
# DocMan Standalone Mode Docker Compose
# Uses nginx as API gateway for simple routing
# ============================================

services:
  # -----------------------------------------
  # Nginx API Gateway (with Frontend)
  # -----------------------------------------
  nginx:
    image: docman-nginx:latest
    container_name: docman-nginx
    ports:
      - "89:89"
    networks:
      - docman-network
    restart: always

# ============================================
# Networks
# ============================================
networks:
  docman-network:
    external: true
    name: docman-network
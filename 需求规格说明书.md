# 文档管理系统需求规格说明书

**版本**：v1.0
**日期**：2026-03-24

---

## 1. 引言

### 1.1 目的
本文档旨在定义文档管理系统的功能和非功能需求，作为系统设计与开发的依据。

### 1.2 范围
系统面向企业级用户，提供文档的创建、存储、检索、版本控制、权限管理及协作功能。

### 1.3 定义与缩略语

| 术语 | 定义 |
|------|------|
| 文档 | 具有唯一标识的电子文件，可包含文本、图片、附件等 |
| 版本 | 文档在生命周期中的某一状态快照 |
| 权限 | 用户对文档的操作能力（读/写/删/管理） |
| MinIO | 对象存储服务，用于存储文档文件 |
| 分类 | 按业务维度对文档进行归类管理的标签体系 |
| 审批流程 | 文档发布或变更需经过多级审核的工作流 |
| CAD | 二维/三维图纸文件的轻量化预览格式 |

---

## 2. 功能需求

### 2.1 文档管理

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-001 | 用户可上传文档（支持 .pdf、.docx、.xlsx、.txt 等格式） | 高 |
| FR-002 | 用户可创建文件夹并支持多级目录结构 | 高 |
| FR-003 | 用户可对文档进行重命名、移动、复制操作 | 高 |
| FR-004 | 用户可下载文档，支持批量下载（打包为 zip） | 中 |
| FR-005 | 用户可彻底删除文档（放入回收站并可恢复） | 高 |

### 2.2 版本控制

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-010 | 文档每次保存/上传自动创建新版本，无需手动触发 | 高 |
| FR-011 | 版本自动记录操作人、操作时间、变更摘要 | 高 |
| FR-012 | 用户可查看任意历史版本并对比差异 | 高 |
| FR-013 | 用户可将文档恢复到任意历史版本 | 高 |
| FR-014 | 编辑过程中自动生成草稿版本（每 5 分钟） | 中 |
| FR-015 | 版本历史永久保留，不得自动删除 | 高 |

### 2.3 全文检索

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-020 | 支持文件名精确匹配和模糊搜索 | 高 |
| FR-021 | 支持文档内容全文检索（PDF、Word、Excel、TXT 等） | 高 |
| FR-022 | 支持按文档类型、创建者、日期范围、分类标签筛选 | 高 |
| FR-060 | 支持检索结果高亮显示匹配关键词 | 中 |
| FR-061 | 用户可查看最近访问/操作的文档列表 | 低 |
| FR-062 | 搜索支持分页展示，默认每页 20 条 | 中 |
| FR-063 | 索引更新延迟不超过 5 分钟（新文档入库后） | 高 |

### 2.4 MinIO 对象存储

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-027 | 系统使用 MinIO 作为文档文件的存储后端 | 高 |
| FR-028 | 支持分桶（Bucket）管理，按业务或租户隔离 | 高 |
| FR-029 | 支持 MinIO 生命周期管理策略（自动归档/清理） | 中 |
| FR-030 | 支持生成预签名 URL 用于临时访问/下载 | 中 |
| FR-031 | 支持文件断点续传（分块上传） | 中 |

### 2.5 分类管理

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-070 | 支持创建多级分类树（层级不限） | 高 |
| FR-071 | 分类支持关联属性（文本、日期、用户等自定义字段） | 中 |
| FR-072 | 文档可同时归属多个分类（多对多关系） | 高 |
| FR-073 | 管理员可维护分类结构（增删改移动） | 高 |
| FR-074 | 支持按分类浏览文档，支持分类统计（文档数量） | 中 |
| FR-075 | 分类可设置默认权限，继承给该分类下的文档 | 中 |

### 2.6 在线编辑 Office

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-080 | 支持在线编辑 Word 文档（.docx） | 高 |
| FR-081 | 支持在线编辑 Excel 文档（.xlsx） | 高 |
| FR-082 | 支持在线编辑 PPT 演示文稿（.pptx） | 中 |
| FR-083 | 支持多人实时协同编辑同一文档 | 高 |
| FR-084 | 编辑过程中自动保存（每 30 秒）并生成版本 | 高 |
| FR-085 | 编辑锁机制：同一用户编辑时阻止其他人强制编辑 | 中 |
| FR-086 | 协同编辑显示用户光标和在线状态 | 中 |
| FR-087 | 支持在线预览 PDF（无需下载） | 高 |

### 2.7 流程审批

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-090 | 支持自定义审批流程模板（流程名称、节点、审批人） | 高 |
| FR-091 | 支持多级审批（串行审批） | 高 |
| FR-092 | 支持会签审批（所有审批人同时通过才通过） | 中 |
| FR-093 | 支持或签审批（任一审批人通过即流转下一节点） | 中 |
| FR-094 | 审批流程可绑定分类（新文档进入指定分类时自动触发） | 中 |
| FR-095 | 审批人可批准、拒绝、驳回（退回上一节点） | 高 |
| FR-096 | 审批完成后自动更新文档状态（草稿/待审批/已发布/已驳回） | 高 |
| FR-097 | 审批记录完整保留（审批人、时间、意见、结果） | 高 |
| FR-098 | 支持审批催办和审批超时提醒 | 低 |

### 2.8 订阅功能

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-100 | 用户可订阅感兴趣的文档、文件夹或分类 | 高 |
| FR-101 | 订阅触发条件：文档新建/修改/删除/审批状态变更 | 高 |
| FR-102 | 支持站内通知和邮件通知两种方式 | 高 |
| FR-103 | 用户可管理订阅列表（新增/取消/修改通知方式） | 中 |
| FR-104 | 支持按订阅频率聚合通知（即时/每日摘要/关闭） | 中 |
| FR-105 | 文档被订阅时，变更自动推送给订阅者 | 高 |

### 2.9 权限管理

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-110 | 支持为文档/文件夹设置访问权限（私有/团队/公开） | 高 |
| FR-111 | 管理员可创建用户组并批量分配权限 | 高 |
| FR-112 | 支持权限继承（子目录继承父目录权限） | 中 |

### 2.10 协作功能

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-120 | 支持文档评论与批注 | 中 |
| FR-121 | 支持文档共享（生成链接分享） | 中 |
| FR-122 | 文档变更时通知相关协作者 | 低 |

### 2.11 系统管理（管理员）

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-130 | 管理员可管理用户账户（增删改查） | 高 |
| FR-131 | 管理员可查看系统操作日志 | 高 |
| FR-132 | 管理员可配置存储空间配额 | 中 |

### 2.12 CAD 图纸预览

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-140 | 支持二维工程图纸在线预览（DWG、DXF、DWF） | 高 |
| FR-141 | 支持三维模型轻量化预览（STEP、IGES、STP、OBJ、3D PDF） | 高 |
| FR-142 | 预览时自动转换 CAD 文件为浏览器可显示的格式 | 高 |
| FR-143 | 支持图纸的基本操作：缩放、平移、旋转、测量距离/角度 | 高 |
| FR-144 | 支持图层显示控制（显示/隐藏指定图层） | 中 |
| FR-145 | 支持图纸剖面视图（三维模型切开查看内部结构） | 中 |
| FR-146 | 支持在图纸上进行批注和标记 | 中 |
| FR-147 | 预览生成缩略图作为列表封面 | 中 |

### 2.13 缓存策略

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-150 | 热点文档数据缓存到 Redis，减少数据库查询 | 高 |
| FR-151 | 文档预览/下载链接短期缓存，避免重复鉴权开销 | 高 |
| FR-152 | 全文检索结果缓存（按搜索条件 hash 缓存） | 中 |
| FR-153 | 用户会话数据缓存（Session 存储于 Redis） | 高 |
| FR-154 | 分类树/权限数据缓存，减少递归查询 | 中 |
| FR-155 | 缓存支持手动刷新和自动过期（默认 1 小时） | 中 |
| FR-156 | 分布式环境下缓存一致性保证 | 中 |

### 2.14 自动版本管理

| 需求编号 | 描述 | 优先级 |
|----------|------|--------|
| FR-160 | 文档每次保存/上传自动创建新版本，无需手动触发 | 高 |
| FR-161 | 版本自动记录：操作人、操作时间、变更摘要 | 高 |
| FR-162 | 文档编辑过程中支持自动草稿版本（每 5 分钟） | 中 |
| FR-163 | 自动清理旧版本：超过 100 个版本时自动归档最早版本 | 中 |
| FR-164 | 版本对比支持按版本号、时间、操作人筛选 | 中 |
| FR-165 | 支持将任意版本设为当前生效版本（回滚） | 高 |
| FR-166 | 版本历史永久保留，不得自动删除（除非用户主动删除） | 高 |

---

## 3. 非功能需求

### 3.1 性能

| 需求编号 | 描述 | 目标值 |
|----------|------|--------|
| NFR-001 | 文档上传，单次操作响应时间 < 2s | - |
| NFR-002 | 全文检索响应时间 < 3s（1万条记录），启用缓存后 < 1s | - |
| NFR-003 | 系统支持至少 100 个并发用户 | - |
| NFR-004 | 热点文档访问（缓存命中）响应时间 < 100ms | - |
| NFR-005 | CAD 图纸预览生成缩略图 < 10s（单文件） | - |
| NFR-006 | 缓存命中率目标 > 70%（针对热门文档） | - |

### 3.2 可用性

| 需求编号 | 描述 |
|----------|------|
| NFR-010 | 系统可用性达到 99.5% |
| NFR-011 | 支持 Chrome、Firefox、Safari、Edge 最新两个版本 |
| NFR-012 | 界面支持中文/英文双语 |

### 3.3 安全

| 需求编号 | 描述 |
|----------|------|
| NFR-020 | 所有数据传输使用 HTTPS 加密 |
| NFR-021 | 用户密码使用 bcrypt 加密存储 |
| NFR-022 | 敏感操作需记录审计日志 |

### 3.4 数据存储

| 需求编号 | 描述 |
|----------|------|
| NFR-030 | 文档原始文件与元数据分离存储 |
| NFR-031 | 支持文件去重（基于 SHA-256 哈希） |
| NFR-032 | 支持数据备份与恢复机制 |

---

## 4. 数据需求

### 4.1 主要实体

| 实体名 | 关键属性 |
|--------|----------|
| 用户 | 用户ID、用户名、密码哈希、邮箱、角色、创建时间 |
| 用户组 | 组ID、组名、描述、创建时间 |
| 文档 | 文档ID、文件名、文件大小、文件类型、存储路径（MinIO）、上传者、创建时间、文档状态 |
| 版本 | 版本ID、文档ID、版本号、文件路径、变更说明、创建时间 |
| 分类 | 分类ID、分类名称、父分类ID、层级、关联属性Schema |
| 文档分类 | 文档ID、分类ID（多对多） |
| 权限 | 权限ID、资源类型、资源ID、用户/用户组、权限级别 |
| 审批流程 | 流程ID、流程名称、分类ID、节点定义（JSON）、创建时间 |
| 审批实例 | 实例ID、文档ID、流程ID、当前节点、状态、发起人、创建时间 |
| 审批记录 | 记录ID、实例ID、审批人、节点、动作（批准/拒绝/驳回）、意见、时间 |
| 订阅 | 订阅ID、用户ID、资源类型、资源ID、触发条件、通知方式、频率 |
| 通知 | 通知ID、订阅ID、用户ID、内容、状态（未读/已读）、创建时间 |
| 回收站 | 记录ID、文档ID、删除时间、删除者 |
| 操作日志 | 日志ID、用户ID、操作类型、资源类型、资源ID、详情、IP地址、时间 |

### 4.2 存储规划

- 单文件最大限制：100MB
- 单用户存储配额：5GB
- 支持的文档格式见附录 A

---

## 5. 用户界面需求

| 需求编号 | 描述 |
|----------|------|
| UI-001 | 清晰的导航结构，支持目录树视图 |
| UI-002 | 拖拽操作支持（上传、移动文件） |
| UI-003 | 响应式布局，支持移动端访问 |
| UI-004 | 操作结果即时反馈（成功/失败提示） |
| UI-005 | 进度指示器（上传/下载进度） |

---

## 6. 验收标准

### 6.1 功能验收

- [ ] 用户完成注册、登录、登出
- [ ] 文档上传、下载、删除功能正常
- [ ] 文档全文检索返回结果准确（文件名+内容）
- [ ] MinIO 存储集成正常，文件正确写入/读取
- [ ] 分类管理功能正常（创建、关联、浏览）
- [ ] 在线编辑 Word/Excel/PPT 功能正常
- [ ] 协同编辑功能正常（多人同时编辑）
- [ ] 流程审批功能正常（创建、流转、批准/拒绝）
- [ ] 订阅通知功能正常（站内信、邮件）
- [ ] 版本历史自动生成，记录操作人和时间
- [ ] CAD 图纸（DWG/STEP 等）在线预览功能正常
- [ ] 三维模型轻量化展示、缩放旋转测量正常
- [ ] 缓存功能正常（Redis 缓存命中后响应加快）
- [ ] 权限控制生效（无权限用户无法访问）
- [ ] 管理员可正常管理用户和查看日志

### 6.2 性能验收

- [ ] 在规定数据量下检索响应时间符合要求
- [ ] 并发测试无性能瓶颈和数据错误
- [ ] 大文件上传稳定无中断
- [ ] 热点文档缓存命中率达标（>70%）
- [ ] CAD 缩略图生成时间符合要求（<10s）

### 6.3 安全验收

- [ ] 未授权用户无法直接访问 API
- [ ] 密码加密存储验证通过
- [ ] 审计日志完整记录关键操作

---

## 附录 A：支持的文档格式

| 类型 | 扩展名 |
|------|--------|
| 文本文档 | .pdf, .docx, .doc, .txt, .rtf, .odt |
| 电子表格 | .xlsx, .xls, .csv, .ods |
| 演示文稿 | .pptx, .ppt, .odp |
| 图片 | .jpg, .jpeg, .png, .gif, .bmp, .webp |
| 压缩包 | .zip, .rar, .7z |
| 二维图纸 | .dwg, .dxf, .dwf, .dgn |
| 三维模型 | .step, .stp, .iges, .igs, .obj, .stl, .3dxml, .u3d, .prt, .asm |
| CAD轻量化 | .pdf（3D PDF）、. IFC |
| 其他 | .html, .xml, .json, .md |

---

**文档结束**
